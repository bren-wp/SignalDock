import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';const store=new Map();const ctx={self:{localStorage:{getItem:k=>store.get(k)||null,setItem:(k,v)=>store.set(k,v)}}};vm.createContext(ctx);vm.runInContext(fs.readFileSync(new URL('../project-manager.js',import.meta.url),'utf8'),ctx);let r=ctx.self.SignalDockProjectManager.create([],{name:'Payments'});assert.equal(r.projects.length,1);r.projects=ctx.self.SignalDockProjectManager.update(r.projects,r.project.id,{baselineName:'prod'});assert.equal(r.projects[0].baselineName,'prod');const imp=ctx.self.SignalDockProjectManager.importJson(ctx.self.SignalDockProjectManager.exportJson(r.projects,r.project.id));assert.equal(imp.activeId,r.project.id);console.log('project-manager-smoke PASS');
